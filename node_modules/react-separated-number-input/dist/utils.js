"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.groupCharacters = groupCharacters;
exports.removeNonNumericChars = removeNonNumericChars;
exports.sum = sum;

/**
 * Splits string into pieces of defined lengths.
 *
 * @param {*} string
 * @param {*} groupLengths
 */
function groupCharacters(string, groupLengths) {
  if (!string || !groupLengths) {
    return [string];
  }

  var i = 0;
  var result = [];
  groupLengths.forEach(function (groupLength) {
    var characterGroup = string && string.slice(i, i + groupLength);

    if (characterGroup) {
      result.push(characterGroup);
    }

    i += groupLength;
  });
  return result;
}

function removeNonNumericChars(string) {
  if (!string) {
    return string;
  }

  return string.replace(/[^\d]/g, '');
}

function sum(arr) {
  return arr.reduce(function (res, el) {
    return res + el;
  }, 0);
}